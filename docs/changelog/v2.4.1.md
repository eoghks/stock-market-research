# v2.4.1 변경 사항 (2026-04-29)

## 버그 수정 / 기능 변경 (PATCH)

### 이메일 발송 엔진 교체 — nodemailer → SendGrid API

**배경:** Naver SMTP 방식은 샌드박스·Cowork 환경에서 포트 차단으로 발송 실패.
SendGrid API Key 방식은 SMTP 포트 불필요, 발송률 높음.

**변경:**
- `scripts/email/send_report.js`: nodemailer 제거 → `@sendgrid/mail` 사용
  - API Key 인증 (`config.sendgrid.api_key`)
  - 첨부 파일 Base64 인코딩 (SendGrid 규격)
  - 발송 실패 시 SendGrid 오류 상세 출력
- `scripts/package.json`: `nodemailer` 제거 → `@sendgrid/mail ^8.1.6` 추가
- `config/email_config.json` (로컬 전용, gitignore): SendGrid 형식으로 교체
  - `provider: "sendgrid"`
  - `sendgrid.api_key`: API 키 (GitHub 미업로드)
  - 발신자: korea4745@gmail.com
  - 수신자: zhfldk7316@naver.com
- `config/email_config.example.json`: SendGrid 예시로 업데이트

**API 키 보안:**
- `config/email_config.json`은 `.gitignore`에 등록 — GitHub에 절대 업로드되지 않음
- `config/email_config.example.json`에는 더미 키만 포함
